# Backend Test Suite - GitHub Actions
# Runs on every push and PR to main branch

name: Backend Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  PYTHON_VERSION: '3.11'
  TESTING: 'true'

jobs:
  # ============================================
  # Unit Tests (Core - Must Pass)
  # ============================================
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-asyncio
      
      - name: Run unit tests
        run: |
          pytest tests/test_payment_service.py tests/test_tier_logic.py -v --tb=short
        env:
          TESTING: 'true'
          STRIPE_SECRET_KEY: 'sk_test_mock'
          STRIPE_WEBHOOK_SECRET: 'whsec_test_mock'

  # ============================================
  # Integration Tests (Allow Failures)
  # These tests require full app context
  # ============================================
  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    continue-on-error: true  # Don't block on failures
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-asyncio
      
      - name: Run integration tests
        run: |
          pytest tests/test_webhook_handler.py tests/test_admin_payment.py -v --tb=short || true
        env:
          TESTING: 'true'
          STRIPE_SECRET_KEY: 'sk_test_mock'
          STRIPE_WEBHOOK_SECRET: 'whsec_test_mock'
          SUPABASE_URL: 'https://mock.supabase.co'
          SUPABASE_KEY: 'mock_key'

  # ============================================
  # Payment Service Tests (Critical - Must Pass)
  # ============================================
  payment-tests:
    name: Payment Tests (Critical)
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-asyncio
      
      - name: Run Stripe payment service tests
        run: |
          pytest tests/test_payment_service.py -v --tb=short
        env:
          TESTING: 'true'
          STRIPE_SECRET_KEY: 'sk_test_mock'
          STRIPE_WEBHOOK_SECRET: 'whsec_test_mock'

  # ============================================
  # Code Quality
  # ============================================
  lint:
    name: Code Quality
    runs-on: ubuntu-latest
    continue-on-error: true
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: Install linters
        run: |
          pip install flake8
      
      - name: Run flake8 (critical errors only)
        run: flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics || true

  # ============================================
  # Security Scan
  # ============================================
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    continue-on-error: true
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: Install pip-audit
        run: pip install pip-audit
      
      - name: Run pip-audit
        run: pip-audit -r requirements.txt || true

  # ============================================
  # Test Summary
  # ============================================
  test-summary:
    name: Test Summary
    runs-on: ubuntu-latest
    needs: [unit-tests, payment-tests]
    if: always()
    
    steps:
      - name: Check test results
        run: |
          echo "## Test Results Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ needs.unit-tests.result }}" == "success" ]; then
            echo "✅ Unit Tests: Passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Unit Tests: Failed" >> $GITHUB_STEP_SUMMARY
          fi
          
          if [ "${{ needs.payment-tests.result }}" == "success" ]; then
            echo "✅ Payment Tests: Passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Payment Tests: Failed" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "ℹ️ Integration tests run separately and may be skipped if dependencies are unavailable." >> $GITHUB_STEP_SUMMARY
      
      - name: Final status
        if: needs.unit-tests.result == 'failure' || needs.payment-tests.result == 'failure'
        run: |
          echo "::error::Critical tests failed!"
          exit 1
