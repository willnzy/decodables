# 状态与版本策略

## 状态枚举

- `draft`: 草稿，未完成或未评审
- `active`: 当前有效版本
- `needs-review`: 需要复核或可能过时
- `archived`: 历史版本或已废弃

## 版本策略

- 采用语义化版本 `X.Y.Z`
  - `X`: 重大调整（不兼容）
  - `Y`: 功能增强（兼容）
  - `Z`: 修订与勘误
- 每次更新必须记录版本与版本日期

## 复核机制

所有 `active` 文档必须包含：

- `最后复核` 日期
- `负责人` 信息
